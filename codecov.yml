coverage:
  status:
    project:
      default:
        target: auto
        # Allow up to a 5% coverage drop relative to the base before failing.
        threshold: 5%
    patch:
      default:
        target: auto
        threshold: 5%
